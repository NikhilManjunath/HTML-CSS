CSS variables are created when there is some features being used very often in the styling of the html page

The CSS variables are created in the root so that they can be used anywhere in the stylesheet

To create a variable -- is used
eg: --primary-color: #f4f4f4;

